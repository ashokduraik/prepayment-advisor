import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.altooxs.prepaymentadvisor',
  appName: 'Prepayment Advisor',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
